name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build test
        run: g++ -std=c++17 tests/test_integrate.cpp -o test

      - name: Run test
        run: ./test